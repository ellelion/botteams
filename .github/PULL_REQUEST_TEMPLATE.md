## Listing

- [ ] I ran this in Grok Bot end to end
- [ ] File is `teams/<slug>.md` or `bots/<slug>.md`
- [ ] `slug` matches the filename
- [ ] `npm run validate` passes

After merge the page is live at `https://botteams.ai/teams/<slug>` or `/bots/<slug>`. There is no second publish step.

## Not a listing

What changed and why.
